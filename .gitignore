node_modules
dist
.env
cache/
*.db
*.db-*
models/
piper/

# Motor de clone de voz (venv Python local, ~6GB) + temporários
tools/clone-venv/
tools/_smoke_*
voice-clones/

# Logs de produção (supervisor start-prod.mjs) + estado do registo de comandos
logs/
commands-state.json

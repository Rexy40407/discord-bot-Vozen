node_modules
dist
.env
cache/
*.db
*.db-*
models/
piper/

# Motor de clone de voz (venv Python local, ~6GB) + temporários
tools/clone-venv/
tools/_smoke_*
voice-clones/

# Sidecar STT Whisper (venv Python local + modelo ~140MB) — instalado por setup-whisper.sh
tools/whisper-venv/

# Sidecar TTS Kokoro (venv Python local + modelo/vozes ~340MB) — descarregados por setup-kokoro.ps1
tools/kokoro-venv/
tools/kokoro-v1.0.onnx
tools/voices-v1.0.bin

# Logs de produção (supervisor start-prod.mjs) + estado do registo de comandos
logs/
commands-state.json

# Cache de áudio sintetizado em runtime (regenera-se; nunca commitar blobs)
audio-cache/

# Scratch de agentes/pessoal e índice do Obsidian (o repo vive dentro da vault)
scratchpad/
_Índice Voxi-bot.md

# Saída da minificação do site (gerada por `npm run build:site`; publicada só em CI)
site-dist/
